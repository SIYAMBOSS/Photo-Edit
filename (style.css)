:root {
    --bg-color: #0f172a;
    --card-bg: #1e293b;
    --primary-color: #38bdf8;
    --accent-color: #fbbf24;
    --text-color: #f8fafc;
}

body {
    margin: 0;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: var(--bg-color);
    color: var(--text-color);
    line-height: 1.6;
}

.container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 20px;
}

header {
    padding: 20px 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

header .container {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo {
    font-size: 24px;
    font-weight: bold;
    letter-spacing: 1px;
}

.logo span { color: var(--primary-color); }

nav ul { list-style: none; display: flex; align-items: center; }
nav ul li { margin-left: 30px; }
nav ul li a { text-decoration: none; color: var(--text-color); font-weight: 500; transition: 0.3s; }
nav ul li a:hover { color: var(--primary-color); }

.btn-premium {
    background: linear-gradient(45deg, #f59e0b, #ef4444);
    padding: 10px 20px;
    border-radius: 50px;
}

.hero-section {
    text-align: center;
    padding: 80px 0;
}

h1 { font-size: 3rem; margin-bottom: 20px; }
.gradient-text {
    background: linear-gradient(to right, #38bdf8, #818cf8);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

.upload-container {
    background: var(--card-bg);
    border: 2px dashed rgba(255, 255, 255, 0.2);
    border-radius: 20px;
    padding: 50px;
    margin: 40px auto;
    max-width: 600px;
    transition: 0.3s;
}

.upload-container:hover { border-color: var(--primary-color); background: #26334d; }

.upload-icon { font-size: 50px; color: var(--primary-color); margin-bottom: 20px; }

.btn-main {
    display: inline-block;
    background: var(--primary-color);
    color: #000;
    padding: 15px 40px;
    border-radius: 10px;
    font-weight: bold;
    cursor: pointer;
    margin-top: 20px;
    transition: 0.3s;
}

.btn-main:hover { transform: translateY(-3px); box-shadow: 0 10px 20px rgba(56, 189, 248, 0.3); }

.features-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
    margin-top: 60px;
}

.feature-card {
    background: var(--card-bg);
    padding: 30px;
    border-radius: 15px;
    transition: 0.3s;
}

.feature-card i { font-size: 30px; color: var(--accent-color); margin-bottom: 15px; }

.feature-card:hover { transform: scale(1.05); background: #2d3a4f; }

footer { text-align: center; padding: 40px 0; opacity: 0.6; font-size: 14px; }
